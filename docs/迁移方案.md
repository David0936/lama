# lama-mumtso 迁移方案

## 当前核查

2026-09-13：远端 `David0936/lama` 为空仓库；本地已克隆作为本设计工作目录。`../lama-mumtso` 工作区干净，HEAD 为 `521238ea21e8ba37398162f2471d37d3ee90b7b7`。本轮没有移动、复制或发布门措资料。

来源：[总库](https://github.com/David0936/lama)、[原项目](https://github.com/David0936/lama-mumtso)。后续迁移前重新检查两端状态。

## 建议方式

目标为 `lama/01_人物/lama-mumtso/`，作为普通受Git跟踪的子目录。迁移保留原Git历史，建议通过不使用 squash 的 git subtree 导入原仓库；不采用需要读者额外初始化的 submodule。这里只是方案，当前同名目录中的 README 是规划占位，导入前需先移除该占位文件/目录并确认无其他内容。

保留现有SKILL.md、references、assets、scripts及内部相对路径，避免第一步就拆坏可运行的Skill。总库人物索引指向其README。原仓库迁移后的维护方式建议为“保留历史和迁移说明，总库单向维护”；旧仓库的删除或归档另行明确，不自动处理。

## 顺序与验收

1. 记录原仓库分支、提交、工作区和远端状态，审计已跟踪文件体积与来源许可；不复制 `.git`、缓存和未跟踪下载物。
2. 清理规划占位并导入历史；验证原提交仍在历史中。对原HEAD跟踪文件与目标目录逐文件比较SHA-256，单独列出任何有意修改。
3. 核查安装说明、GitHub原始文件链接、跨目录跳转、脚本工作目录和准备资料脚本；保留现有下载入口的过渡说明。
4. 为原资料分配总库ID与出处记录。共享文献先引用现有位置，等双向链接完成后再去重，不第一步大规模拆迁。
5. 核验原Skill加载、全文检索与三语入口；运行仓库现有适用检查，确认移位不改变功能。
6. 按迁移任务的实际范围提交至lama总库，维持届时已确定的仓库设置；当前迁移不以私有化为前提。原仓库的迁移指引与是否归档另行处理。迁移完成记录包括旧commit、新commit、差异与验证证据。

验收：原内容可读、历史可追溯、旧功能可运行、链接可用、无第二个编辑中的副本、无无意增加的大文件。框架设计完成不等于上述迁移已经执行。

## v0.6 范围更正

当前迁移与整理对象是公开资料，不以私有化、老师授权或加密为前提。私有典藏架构保留为未来产品方案；原公开项目的迁移继续按前述内容与功能验证执行。此澄清本身不更改远端可见性，也不启动迁移。
